# 拼豆网站 → Web App：Codex 提示词与操作指引

> 依据抖音视频《不懂后端，我用 AI 把网站做成了软件产品》（Chestnut）总结。
> 目标：把现有「十三拼豆坊 · 拼豆图纸转换器」静态网站，用 Codex + Zion 改造成能注册、登录、付费的软件产品。

---

## 一、视频的核心方法（先看懂再动手）

视频的核心：**简历型网站（只有前端）≠ Web App（前端 + 后端规则 + 数据库）**。
用 **Codex（负责写代码）+ Zion 平台（负责后端：用户系统、数据库、兑换码、订单、支付）** 两步完成：

| 步骤 | 做什么 | 你的角色 |
|---|---|---|
| 1 | 打开 Codex，输入提示词，按 AI 要求完成 **Zion 的登录和授权**（Zion Plugin） | 照做授权 |
| 2 | **先不让 AI 写代码**，让它当"产品经理"，把你的想法拆成具体功能 + 搭建顺序，确认后再开工 | 审核计划 |
| 3 | 让 Codex **一个功能一个功能**做：做一测一，确认没问题再做下一个 | 测试 + 点确认 |
| 4 | 验收时去 **Zion 后台**看数据库和后端规则（可视化，非技术人也看得懂），有问题在后台人工处理 | 后台查看/处理 |

**视频里的两个必说提醒：**
1. **必须一开始就告诉它：前端用 React 框架 + Vite 搭建本地项目**（这句话不能省）。
2. **必须强调：所有接口必须真实可用，不能只做页面演示**（AI 很喜欢偷懒，只画个好看的登录页但按钮背后没东西）。

**视频的产品化路径（两种领取方式）：**
- 方式一：**兑换码** → 适合从小红书 / B 站来的用户，店铺自动发货发兑换码，用户登录网站自助领取。
- 方式二：**网站直接下单** → 接入微信 / 支付宝商家号，付款后自动发权益，不需要人工核实订单。

---

## 二、给 Codex 的完整提示词（直接复制）

> 使用方法：在 Codex 聊天框粘贴下面内容，回车。Codex 会先让你完成 Zion 登录授权，然后给出功能拆解计划；你审核确认后，**让它一次只做一个功能**。

```
【角色】
你是全栈工程师 + 产品经理。我要把现有的「十三拼豆坊 · 拼豆图纸转换器」网站改造成一个可以注册、登录、付费的 Web App 软件产品，请帮我完成这个改造。

【后端平台】
使用 Zion（已安装 Zion Plugin），由它承担：用户注册/登录、数据库、兑换码、订单、支付。完成后我会去 Zion 后台查看数据库和后端规则。

【现有资产（请复用，不要重写）】
1. 拼豆图纸转换算法库：项目里的 bead-core-main（TypeScript 纯算法，提供 runPipeline 等函数），直接作为依赖引入前端使用。
2. AI Q 版人物生成：目前通过后端代理 Seedream 接口（netlify/functions/seedream.js，需 ARK_API_KEY）调用，请保留这个能力，接入用户权益体系。
3. 现有页面视觉风格可参考 studio/ 目录：纸感、米色暖色调、MARD 色卡、十三拼豆坊品牌感，新界面尽量沿用。

【产品功能拆解（请按这个顺序逐个实现，每做完一个就停下等我确认，确认后再做下一个）】
1. 用户系统：注册 / 登录 / 退出，用 Zion 用户系统；建立 users 数据表（含剩余图纸导出次数、AI 生图次数、会员状态）。
2. 用户中心：登录后可看到我的账户（剩余导出次数、AI 生图次数、会员状态、兑换记录）。
3. 兑换码系统：管理员可生成兑换码（区分：按次导出包 / 永久会员 / Q 版生图次数包）；用户输入兑换码激活后权益到账；数据库记录兑换码由谁、何时使用。
4. 图纸导出权限：未登录/未付费时预览带水印，登录且有对应次数才能导出无水印 PNG / CSV。
5. AI Q 版人物生成：接入用户权益，每次消耗对应次数，扣减并记录到用户账户。
6. 管理员后台：生成兑换码、查看用户列表、查看兑换/生图/订单记录。
7. 支付（最后再做）：接入微信 / 支付宝商家号，支持用户网站直接下单，付款成功后自动发放对应权益或兑换码。

【硬性要求（必须遵守）】
- 前端必须使用 React 框架，用 Vite 搭建本地项目。
- 所有接口必须真实可用、真正读写 Zion 数据库，禁止只做静态页面演示（比如登录按钮背后必须是真实的登录逻辑）。
- 登录、注册、兑换、扣次数、支付回调这些都要真实跑通，而不是画个界面。
- 遇到需要我确认的决策（比如页面布局、付费套餐定价）先问我，不要自作主张。
```

---

## 三、操作提醒（来自视频的经验）

1. **一次只做一个功能**，不要一口气让 AI 全做完。做完一个 → 你亲自测试（注册 → 登录 → 兑换 → 导出）→ 没问题再继续。这样报错容易定位。
2. **少跟 AI 反复扯皮**。出问题直接去 Zion 后台看数据库和"后端规则"，都是可视化配置，能看懂、能人工改。
3. **开头两句话千万别漏**：①前端用 React + Vite；②接口必须真实可用不能只做演示。
4. **支付环节**需要你提前办好**微信 / 支付宝商家号**（个体户或公司资质），这是唯一需要你在 Codex 之外准备的东西。
5. **变现路径建议**：先用兑换码跑通"自动发货"（适合小红书/B 站引流），再上在线支付，风险更小。
