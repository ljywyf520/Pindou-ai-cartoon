/** 降采样时单格在源图上的像素边界（无间隙、无重叠） */
export declare function cellBounds(imgWidth: number, imgHeight: number, gridWidth: number, gridHeight: number, col: number, row: number): {
    x0: number;
    y0: number;
    x1: number;
    y1: number;
};
//# sourceMappingURL=cellBounds.d.ts.map